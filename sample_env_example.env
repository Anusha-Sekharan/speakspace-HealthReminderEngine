# Example Environment Variables
# Copy to .env

# Port for the FastAPI server (default is 8000)
PORT=8000

# Optional: Path overrides if not using default project root
# GOOGLE_APPLICATION_CREDENTIALS=./credentials.json
